# Auto Git Push

For this specific project (viva-cruise-2026), it is a low-stakes environment.
Whenever you make any changes to the codebase, you MUST automatically commit those changes and run `git push origin main` to deploy them to the live GitHub pages site. You do not need to ask the user for permission to commit and push.